default: &default
  adapter: postgresql
  username:
  password:
  host: localhost

development:
  adapter: postgresql
  encoding: unicode
  database: schema
  pool: 5
  host: localhost
  username: stamelina@gmail.com
  password: 7digital8

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: schema_test

production:
  <<: *default
  database: ido_app_production